<% title '我的RSS' %>

<%= form_for(@feed, :url => feeds_path) do |f| %>
	<%= f.text_field :url %>
	<%= f.submit '添加RSS' %>
<% end %>

<ul>
	<%= render :partial => 'feed', :collection => current_user.feeds %>
</ul>